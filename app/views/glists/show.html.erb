<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @glist.name %>


<%= link_to 'Edit', edit_glist_path(@glist) %> |
<%= link_to 'Back', glists_path %>
</p>
<br>
<table>
  <thead>
    <tr>
      <th>Item Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @all_line_items.each do |line_item| %>
      <tr>
        <td><%= line_item.item.name %></td>
        <td><%= link_to 'Show', line_item %></td>
        <td><%= link_to 'Edit', edit_line_item_path(line_item) %></td>
        <td><%= link_to 'Destroy', line_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
      </tbody>
</table>
<br>
<%= link_to 'New Line item', new_line_item_path %>
